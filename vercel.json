{
  "github": {
    "silent": true
  },
  "framework": "vitepress",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "git fetch --unshallow && PLATFORM=vercel corepack pnpm build --base /",
  "cleanUrls": true,
  "redirects": [
    {
      "source": "/blog/:path*",
      "destination": "/:path*"
    },
    {
      "source": "/posts/:path*",
      "destination": "/breves/:path*"
    }
  ],
  "headers": [
    {
      "source": "/feed.rss",
      "headers": [
        {
          "key": "Content-Type",
          "value": "application/rss+xml; charset=utf-8"
        }
      ]
    }
  ]
}
